# Required
HANDY_MASTER_SECRET=replace-with-a-strong-secret

# 服务器公网 IP 或域名，所有公网访问地址自动基于此生成
# 例如: HOST_IP=136.111.18.31 或 HOST_IP=example.com
HOST_IP=localhost

# 对外暴露的端口（只需开放这一个）
WEB_PORT=80

# Service credentials
POSTGRES_PASSWORD=postgres
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
S3_BUCKET=happy

# 以下 URL 留空则自动由 HOST_IP 拼接，通常不需要手动设置
HAPPY_SERVER_URL=
API_PUBLIC_URL=
S3_PUBLIC_URL=

# Optional Web build args
POSTHOG_API_KEY=
REVENUE_CAT_STRIPE=
